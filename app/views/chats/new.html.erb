<div class="flex justify-center items-center h-screen items-center">
  <%= form_with(model: Chat.new, url: chat_path, method: :post) do |form| %>
    <%= form.submit "Start", class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
  <% end %>
</div>
